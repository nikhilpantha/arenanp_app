/**
 * Page sizes for paginated / infinite-loading lists. Use these named constants
 * everywhere instead of inline magic numbers so a page size is tuned in one place.
 */

/** Default page size for infinite-loading lists (e.g. venue customers). */
export const PAGE_SIZE = 20;

/** Smaller page size for compact or secondary lists. */
export const COMPACT_PAGE_SIZE = 10;
